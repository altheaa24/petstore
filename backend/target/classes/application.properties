# PostgreSQL Configuration (Commented out for local H2 fallback)
# spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/petstore}
# spring.datasource.username=${DB_USER:postgres}
# spring.datasource.password=${DB_PASSWORD:root123}
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# H2 In-Memory Database for Local Run
spring.datasource.url=jdbc:h2:mem:petstore;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=${PORT:8080}
